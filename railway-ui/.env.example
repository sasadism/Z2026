# Base URL of your existing ZEUS Cloudflare Worker (no trailing slash).
# This is the ONLY required setting.
ZEUS_ORIGIN=https://your-worker.workers.dev

# Optional: port to listen on. Railway sets PORT automatically, so leave unset there.
# PORT=3000
